import Immutable from 'immutable';
import * as ActionsTypes from '../actions/ActionsTypes';

export default function wrokHours(state, action) {
  switch(action.type) {
    default: {
      return state;
    }
  }
}